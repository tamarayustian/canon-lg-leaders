export type Submission = {
  member: string;
  trait: string;
  memory: string;
  message: string;
  imageUrl?: string;
};

export const MALE_LEADER_NAME = 'Makoto';
export const FEMALE_LEADER_NAME = 'Leena';

export const maleSubmissions: Submission[] = [];

export const femaleSubmissions: Submission[] = [];
